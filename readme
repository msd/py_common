# Install

## Pip git (online)

pip install git+https://github.com/msd/py_common

## Pip manual (offline)

(optional) enable virtual environment

clone repository

`git clone https://github.com/msd/py_common msd_py_common`

install

`pip install msd_py_common`

or install as an editable package

`pip install -e msd_py_common`

# Documentation TODO

Just some code that I find useful and can be potentially be used in many of my projects

Webpage
    Lazy HTML and BeautifulSoup
    Accepts an optional requests.Session

    TODO: write to a file

download_file
    Download a file using requests

